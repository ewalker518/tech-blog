<div class="card comment">
    {{#each this}}
    <h4 class="card-header">Comments:</h4>
    <div class="content">
        {{comment_text}}
    </div>
    <div>
        {{user.username}}, {{format_date created_at}}
    </div>
    {{/each}}
</div>