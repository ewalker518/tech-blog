<div class="card post">
    <div class="card-header">
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
    <div class="card-body">
        <p>{{post.content}}</p>
    </div>
</div>